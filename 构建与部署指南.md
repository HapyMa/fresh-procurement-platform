# 生鲜采购平台 - 构建与部署指南

## 一、环境要求

### 后端（Spring Boot）
- JDK 11 或以上
- Gradle 7.6+（项目自带 wrapper）
- 无需安装 MySQL（默认使用 H2 内存数据库）

### Android 端
- Android Studio 2022.1+
- JDK 17
- Android SDK 34
- 最低支持 Android 8.0（API 26）

---

## 二、后端启动

### 方式一：使用 Gradle Wrapper（推荐）

```bash
cd fresh-backend

# Windows
gradlew.bat bootRun

# Mac/Linux
chmod +x gradlew
./gradlew bootRun
```

### 方式二：使用 IDE

1. 用 IntelliJ IDEA 或 Spring Tool Suite 打开 `fresh-backend` 目录
2. 等待 Maven/Gradle 依赖下载完成
3. 运行 `FreshProcurementApplication.java` 的 main 方法

### 启动成功标志

```
Started FreshProcurementApplication in X.XXs
```

后端运行在 `http://localhost:8080`

### 测试数据

后端启动后会自动初始化以下测试数据：

| 测试账号 | 手机号 | 密码 | 角色 |
|---------|--------|------|------|
| 老王餐厅 | 13800138000 | 123456 | 采购商 |
| 学校食堂 | 13800138001 | 123456 | 采购商 |
| 社区团购 | 13800138002 | 123456 | 采购商 |
| XX蔬菜批发 | 13900139000 | 123456 | 供应商 |
| YY农产品 | 13900139001 | 123456 | 供应商 |

### H2 数据库控制台

访问 `http://localhost:8080/h2-console`
- JDBC URL: `jdbc:h2:mem:freshdb`
- 用户名: `sa`
- 密码: (空)

---

## 三、Android 端构建 APK

### 方式一：Android Studio 构建（推荐）

1. 用 Android Studio 打开 `FreshProcurementApp` 目录
2. 等待 Gradle Sync 完成
3. 菜单：Build → Build Bundle(s) / APK(s) → Build APK(s)
4. APK 输出路径：`app/build/outputs/apk/debug/app-debug.apk`

### 方式二：命令行构建

```bash
cd FreshProcurementApp

# Windows
gradlew.bat assembleDebug

# Mac/Linux
chmod +x gradlew
./gradlew assembleDebug
```

### 连接后端

默认配置连接模拟器本机后端（`http://10.0.2.2:8080/`）。

**真机测试时**，修改 `app/src/main/java/com/fresh/procurement/data/remote/RetrofitClient.kt` 中的 BASE_URL 为电脑的局域网 IP：

```kotlin
private const val BASE_URL = "http://192.168.1.100:8080/"
```

---

## 四、API 接口列表

### 认证接口（无需 Token）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/v1/auth/register | 注册 |
| POST | /api/v1/auth/login | 登录 |

### 采购商接口（需要 Token）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/v1/buyer/demands | 发布需求 |
| GET | /api/v1/buyer/demands | 需求列表 |
| GET | /api/v1/buyer/demands/{id} | 需求详情 |
| POST | /api/v1/buyer/demands/{id}/select-quote | 选择报价 |
| POST | /api/v1/buyer/demands/{id}/confirm-receipt | 确认收货 |

### 供应商接口（需要 Token）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/v1/supplier/demand-groups | 合并需求列表 |
| GET | /api/v1/supplier/demand-groups/{id} | 合并需求详情 |
| POST | /api/v1/supplier/demand-groups/{id}/quotes | 提交报价 |
| GET | /api/v1/supplier/quotes | 我的报价 |
| GET | /api/v1/supplier/orders | 我的订单 |
| GET | /api/v1/supplier/pack/pending-list | 待打包列表 |
| POST | /api/v1/supplier/pack/{id}/start | 开始分拣 |
| POST | /api/v1/supplier/pack/{id}/complete | 完成打包 |
| POST | /api/v1/supplier/ship/{id} | 发货 |

### 公共接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/v1/categories | 商品分类 |
| GET | /api/v1/cities | 城市列表 |
| GET | /api/v1/user/profile | 用户信息 |
| GET | /api/v1/user/addresses | 地址列表 |

---

## 五、项目结构

```
workspace/
├── FreshProcurementApp/          # Android 客户端
│   ├── app/src/main/java/com/fresh/procurement/
│   │   ├── data/                 # 数据层
│   │   │   ├── model/            # 数据模型
│   │   │   ├── remote/           # API 客户端
│   │   │   └── repository/       # 数据仓库
│   │   ├── di/                   # 依赖注入
│   │   └── ui/                   # UI 层
│   │       ├── navigation/       # 导航
│   │       ├── screens/          # 页面
│   │       ├── theme/            # 主题
│   │       └── viewmodel/        # ViewModel
│   └── app/build.gradle.kts
│
├── fresh-backend/                # Spring Boot 后端
│   ├── src/main/java/com/fresh/procurement/
│   │   ├── config/               # 配置
│   │   ├── controller/           # API 控制器
│   │   ├── dto/                  # 数据传输对象
│   │   ├── entity/               # JPA 实体
│   │   ├── repository/           # 数据访问
│   │   ├── security/             # 安全认证
│   │   └── service/              # 业务逻辑
│   └── build.gradle
│
├── database_schema.sql           # 数据库设计文档
└── api_document.md               # API 接口文档
```

---

## 六、切换到 MySQL（生产环境）

修改 `fresh-backend/src/main/resources/application.yml`：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fresh_db?useSSL=false&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: your_password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  sql:
    init:
      mode: never
```

在 `build.gradle` 中取消 H2 依赖，保留 MySQL 依赖。
